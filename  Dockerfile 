# 使用官方 Python 基底映像檔
FROM python:3.11-slim

# 設定工作目錄
WORKDIR /app

# 複製 requirements.txt 並安裝相依套件
COPY requirements.txt .
RUN pip install --upgrade pip && pip install -r requirements.txt

# 複製全部的後端原始碼進容器
COPY . .

# 啟動 Flask 應用（這裡假設你的主程式叫 app.py）
CMD ["python", "app.py"]
